#include "main.h"
/**
 * _islower - checks for lowercase characters
 * @c: character to be checked for
 * Return: if lowercase return c, if otherwise 0
 */
int _islower(int c);
{
	return (c >= 'a' && c <= 'z');
}
